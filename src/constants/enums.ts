export enum NumberTypes {DTelNumber = -1, GuildNumber, DMNumber};
export enum SuffixTypes {DTelNumber, userMention, channelMention, Number, text};

export enum Languages {en, fr, de, nl};

export enum Phones {default, donator, support};

export enum UserPerms {default, donator, admin, support, supervisor, boss};
export enum NumberPerms {none, default, active, vip};
